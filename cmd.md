# CMD

```bash
  flutter clean && flutter pub get
  cd ios && rm -rf Pods/ Podfile.lock && pod install && cd .. 
  flutter clean && flutter pub get
```

```bash
   flutter clean && flutter pub get
```

```bash
   flutter doctor -v
```

```bash
   darwin/build.sh
```

```bash
   darwin/create_xcframework.sh
```

```bash
  flutter pub add objectbox objectbox_flutter_libs
  flutter pub add path_provider path
  flutter pub add flutter_bloc flutter_hooks
  flutter pub add equatable dart_mappable
  flutter pub add --dev build_runner objectbox_generator dart_mappable_builder
```